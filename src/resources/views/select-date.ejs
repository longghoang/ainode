<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Doanh Thu</title>
    <link rel="stylesheet" href="/css/reset.css">
    <link rel="stylesheet" href="/css/header.css">
    <link rel="stylesheet" href="/css/capture.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">
    <%- include('partials/header') %>
    <style>
        body {
            margin-top: 100px;
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            background-color: #f4f7f9;
            color: #333;
         
            padding: 40px; /* Tăng padding */
            display: flex;
            flex-direction: column;
            min-height: 100vh;
        }

        h3 {
            font-size: 28px;
            color: #2c3e50;
            margin-bottom: 20px;
            text-align: center;
        }

        form {
            max-width: 600px; /* Tăng chiều rộng */
            margin: 0 auto;
            padding: 20px;
            background: #fff;
            border-radius: 8px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
            display: flex;
            flex-direction: column;
            gap: 20px;
        }

        label {
            display: block;
            font-size: 16px;
            color: #2c3e50;
            margin-bottom: 8px;
        }

        input[type="date"] {
            width: calc(100% - 22px);
            padding: 10px;
            font-size: 16px;
            border: 1px solid #dcdcdc;
            border-radius: 4px;
            box-sizing: border-box;
        }

        .btn-secondary {
            background: none; /* Bỏ màu nền */
            color: #27ae60; /* Màu chữ xanh lá cây */
            border: 1px solid #27ae60; /* Đường viền xanh lá cây */
            border-radius: 4px;
            padding: 12px 20px;
            font-size: 16px;
            cursor: pointer;
            transition: background-color 0.3s ease, transform 0.3s ease;
            display: inline-flex;
            align-items: center;
            text-decoration: none;
            font-weight: bold;
        }

        .btn-secondary:hover {
            background-color: #27ae60; /* Xanh lá cây khi hover */
            color: #fff;
            transform: translateY(-2px);
        }

        .btn-secondary:focus {
            outline: none;
        }

        .btn-back {
            color: #3498db; /* Màu chữ xanh dương */
            border-color: #3498db; /* Đường viền xanh dương */
        }

        .btn-back:hover {
            background-color: #3498db; /* Xanh dương khi hover */
            color: #fff;
        }

        .btn-back i {
            margin-right: 8px; /* Khoảng cách giữa icon và văn bản */
        }

        .btn-report i {
            margin-right: 8px; /* Khoảng cách giữa icon và văn bản */
        }
    </style>
</head>
<body>
    <h3>Báo cáo doanh thu</h3>
    <form method="GET" action="/revenue/report-revenues">
        
        <input type="date" id="date" name="date" required>
        <div class="button-group">
            <button type="submit" class="btn-secondary btn-report"><i class="fas fa-file-alt"></i> Xem báo cáo</button>
            <a class="btn-secondary btn-back" href="/revenue"><i class="fas fa-arrow-left"></i> Quay lại</a>
        </div>
    </form>

</body>
</html>
