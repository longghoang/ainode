<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Báo Cáo Doanh Thu</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">
    <link rel="stylesheet" href="/css/reset.css">
    <link rel="stylesheet" href="/css/header.css">
    <link rel="stylesheet" href="/css/capture.css">
    <%- include('partials/header') %>
    <style>
        body {
            margin-top: 100px;
            font-family: Arial, sans-serif;
            background-color: #f7f9fc;
            color: #333;
            padding: 20px;
        }

        .report-container {
            
            background-color: #fff;
            border-radius: 8px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
            padding: 20px;
            max-width: 600px;
            margin: 40px auto; /* Center the container */
        }

        h3 {
            font-size: 24px;
            color: #0056b3;
            margin-bottom: 20px;
            text-align: center;
        }

        p {
            font-size: 18px;
            text-align: center;
            margin: 10px 0;
        }

        .btn-secondary {
            background: none; /* Bỏ màu nền */
            color: #27ae60; /* Màu chữ xanh lá cây */
            border: 1px solid #27ae60; /* Đường viền xanh lá cây */
            border-radius: 4px;
            padding: 12px 20px;
            font-size: 16px;
            cursor: pointer;
            transition: background-color 0.3s ease, transform 0.3s ease;
            display: inline-flex;
            align-items: center;
            text-decoration: none;
            font-weight: bold;
        }

        .btn-secondary:hover {
            background-color: #27ae60; /* Xanh lá cây khi hover */
            color: #fff;
            transform: translateY(-2px);
        }

        .btn-secondary:focus {
            outline: none;
        }

        .btn-back {
            color: #3498db; /* Màu chữ xanh dương */
            border-color: #3498db; /* Đường viền xanh dương */
        }

        .btn-back:hover {
            background-color: #3498db; /* Xanh dương khi hover */
            color: #fff;
        }

        .btn-back i {
            margin-right: 8px; /* Khoảng cách giữa icon và văn bản */
        }

        .btn-report i {
            margin-right: 8px; /* Khoảng cách giữa icon và văn bản */
        }
        .container {
            text-align: center;
            margin-top: 20px;
        }
    </style>
</head>
<body>
    <div class="report-container">
        <h3>Doanh thu ngày <%= date %></h3>
        <p>Tổng doanh thu: <%= totalRevenue %> VNĐ</p>
        <p>Số lượng xe: <%= totalVehicles %></p>

        <!-- Thêm thông tin doanh thu khác nếu cần -->
        <div class="container">
            <a class="btn-secondary" href="/revenue/report-revenue"><i class="fas fa-arrow-left"></i> Quay lại</a>
        </div>
    </div>
</body>
</html>
